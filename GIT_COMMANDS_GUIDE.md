# 📚 Полное руководство по Git командам для совместной работы

## 🎯 Основные команды для получения и внесения изменений

---

## 📥 ПОЛУЧЕНИЕ ИЗМЕНЕНИЙ (от друга)

### 1. Получить изменения с GitHub

```bash
git pull
```

**Что делает:**
- Скачивает все изменения с GitHub
- Автоматически объединяет их с вашими локальными файлами
- Обновляет ваш проект до последней версии

**Когда использовать:**
- Перед началом работы (чтобы иметь актуальную версию)
- Когда друг сделал `git push`
- Когда хотите получить последние изменения

**Пример:**
```bash
# Друг сделал изменения и отправил их
# Вы хотите получить эти изменения:
git pull
```

---

### 2. Посмотреть изменения перед получением

```bash
# Сначала посмотреть, что изменилось (без скачивания)
git fetch

# Посмотреть разницу между вашей версией и GitHub
git diff origin/main
```

**Что делает:**
- `git fetch` — скачивает информацию об изменениях, но не применяет их
- `git diff` — показывает, какие файлы изменились

**Когда использовать:**
- Когда хотите посмотреть, что изменилось, перед `git pull`
- Чтобы убедиться, что изменения безопасны

---

### 3. Получить изменения с конкретной ветки

```bash
git pull origin main
```

**Что делает:**
- Получает изменения из ветки `main` на GitHub

**Когда использовать:**
- Если работаете с несколькими ветками
- По умолчанию `git pull` делает то же самое

---

## 📤 ВНЕСЕНИЕ ИЗМЕНЕНИЙ (отправить другу)

### 1. Посмотреть, что изменилось

```bash
git status
```

**Что делает:**
- Показывает список измененных файлов
- Показывает файлы, которые готовы к отправке
- Показывает файлы, которые не отслеживаются Git

**Когда использовать:**
- Перед каждым коммитом
- Чтобы увидеть, какие файлы вы изменили

**Пример вывода:**
```
On branch main
Changes not staged for commit:
  modified:   components/Game.tsx
  modified:   utils/renderUtils.ts

Untracked files:
  new-file.ts
```

---

### 2. Добавить файлы в коммит

```bash
# Добавить все измененные файлы
git add .

# Или добавить конкретный файл
git add components/Game.tsx

# Или добавить несколько файлов
git add components/Game.tsx utils/renderUtils.ts
```

**Что делает:**
- Подготавливает файлы к коммиту
- `git add .` — добавляет все измененные файлы в текущей папке

**Когда использовать:**
- После того, как вы изменили файлы
- Перед `git commit`

**Пример:**
```bash
# Вы изменили Game.tsx и renderUtils.ts
git add .
# Теперь эти файлы готовы к коммиту
```

---

### 3. Создать коммит (сохранить изменения)

```bash
git commit -m "Описание ваших изменений"
```

**Что делает:**
- Сохраняет изменения локально с описанием
- Создает "снимок" вашего кода в этот момент

**Когда использовать:**
- После `git add`
- Когда хотите сохранить изменения

**Примеры хороших описаний:**
```bash
git commit -m "Добавил анимацию для торговца"
git commit -m "Исправил баг с отображением NPC"
git commit -m "Обновил текстуры для горожанина"
git commit -m "Добавил новую функцию редактора карт"
```

**Примеры плохих описаний (избегайте):**
```bash
git commit -m "изменения"          # Слишком общее
git commit -m "fix"                # Непонятно что исправлено
git commit -m "asdfghjkl"         # Бессмысленно
```

---

### 4. Отправить изменения на GitHub

```bash
git push
```

**Что делает:**
- Отправляет ваши коммиты на GitHub
- Делает изменения доступными для друга

**Когда использовать:**
- После `git commit`
- Когда хотите поделиться изменениями с другом

**Пример:**
```bash
# Вы сделали изменения и сохранили их
git add .
git commit -m "Добавил новую функцию"
git push
# Теперь друг может получить эти изменения через git pull
```

---

### 5. Отправить в конкретную ветку

```bash
git push origin main
```

**Что делает:**
- Отправляет изменения в ветку `main` на GitHub

**Когда использовать:**
- По умолчанию `git push` делает то же самое
- Если работаете с несколькими ветками

---

## 🔄 ПОЛНЫЙ ЦИКЛ РАБОТЫ

### Типичный рабочий процесс:

```bash
# 1. Получить последние изменения (ПЕРЕД НАЧАЛОМ РАБОТЫ)
git pull

# 2. Работаете над кодом (редактируете файлы в Cursor)

# 3. Посмотреть, что изменилось
git status

# 4. Добавить изменения
git add .

# 5. Сохранить изменения с описанием
git commit -m "Описание того, что вы сделали"

# 6. Отправить изменения на GitHub
git push

# 7. Друг получает изменения
# (Друг выполняет: git pull)
```

---

## 🛠️ ДОПОЛНИТЕЛЬНЫЕ КОМАНДЫ

### Посмотреть историю коммитов

```bash
git log
```

**Что делает:**
- Показывает список всех коммитов
- Показывает автора, дату и описание каждого коммита

**Выход из просмотра:** Нажмите `q`

---

### Посмотреть изменения в конкретном файле

```bash
git diff components/Game.tsx
```

**Что делает:**
- Показывает, какие строки вы изменили в файле
- Показывает удаленные (красные) и добавленные (зеленые) строки

**Когда использовать:**
- Чтобы увидеть детали изменений перед коммитом

---

### Отменить изменения в файле (еще не добавлены)

```bash
# Отменить изменения в конкретном файле
git checkout -- components/Game.tsx

# Или (новая версия Git)
git restore components/Game.tsx
```

**Что делает:**
- Возвращает файл к последней сохраненной версии
- **ВНИМАНИЕ:** Все ваши изменения будут потеряны!

**Когда использовать:**
- Когда вы случайно испортили файл
- Когда хотите откатить изменения

---

### Убрать файл из подготовки (но сохранить изменения)

```bash
git reset HEAD components/Game.tsx
```

**Что делает:**
- Убирает файл из `git add`, но сохраняет ваши изменения в файле

**Когда использовать:**
- Когда вы случайно добавили файл в `git add`
- Когда хотите добавить файл позже

---

### Отменить последний коммит (но сохранить изменения)

```bash
git reset --soft HEAD~1
```

**Что делает:**
- Отменяет последний коммит
- Сохраняет все изменения в файлах
- Позволяет переделать коммит

**Когда использовать:**
- Когда забыли добавить файл в коммит
- Когда хотите изменить описание коммита

---

## ⚠️ РАЗРЕШЕНИЕ КОНФЛИКТОВ

### Что такое конфликт?

Конфликт возникает, когда вы и друг изменили **одну и ту же строку** в файле.

### Как разрешить конфликт:

```bash
# 1. Попробовать получить изменения
git pull

# 2. Если конфликт:
# Git покажет сообщение:
# "CONFLICT (content): Merge conflict in components/Game.tsx"
```

**В файле с конфликтом вы увидите:**

```typescript
<<<<<<< HEAD
// Ваш код
const myVariable = "моя версия";
=======
// Код друга
const myVariable = "версия друга";
>>>>>>> origin/main
```

**Как исправить:**

1. **Откройте файл в Cursor**
2. **Найдите маркеры конфликта** (`<<<<<<<`, `=======`, `>>>>>>>`)
3. **Выберите нужный код** (ваш, друга, или объедините оба)
4. **Удалите маркеры** (`<<<<<<<`, `=======`, `>>>>>>>`)
5. **Сохраните файл**

**Пример решения:**

```typescript
// БЫЛО (конфликт):
<<<<<<< HEAD
const speed = 5;
=======
const speed = 10;
>>>>>>> origin/main

// СТАЛО (решили использовать версию друга):
const speed = 10;
```

**После решения конфликта:**

```bash
# 3. Добавить исправленный файл
git add components/Game.tsx

# 4. Завершить объединение
git commit -m "Решил конфликт в Game.tsx"

# 5. Отправить изменения
git push
```

---

## 📋 ЧЕКЛИСТ ДЛЯ ЕЖЕДНЕВНОЙ РАБОТЫ

### Перед началом работы:

- [ ] `git pull` — получить последние изменения
- [ ] `git status` — проверить, что все чисто

### Во время работы:

- [ ] Регулярно сохраняйте файлы (Ctrl+S)
- [ ] Делайте коммиты часто (каждые 30-60 минут)

### После работы:

- [ ] `git status` — посмотреть, что изменилось
- [ ] `git add .` — добавить все изменения
- [ ] `git commit -m "Описание"` — сохранить с описанием
- [ ] `git push` — отправить на GitHub

---

## 🎯 ПРИМЕРЫ СЦЕНАРИЕВ

### Сценарий 1: Вы начинаете работу утром

```bash
# 1. Получить изменения, которые друг сделал ночью
git pull

# 2. Начать работать
# (редактируете файлы в Cursor)

# 3. Сохранить и отправить изменения
git add .
git commit -m "Добавил новую функцию атаки"
git push
```

---

### Сценарий 2: Друг работает над тем же файлом

```bash
# 1. Вы делаете изменения
git add .
git commit -m "Изменил Game.tsx"
git push

# 2. Друг тоже изменил Game.tsx и делает:
git pull
# Конфликт!

# 3. Друг решает конфликт (выбирает нужный код)
# 4. Друг завершает:
git add components/Game.tsx
git commit -m "Решил конфликт"
git push

# 5. Вы получаете исправленную версию:
git pull
```

---

### Сценарий 3: Вы случайно испортили файл

```bash
# 1. Посмотреть, что изменилось
git status

# 2. Отменить изменения в файле
git restore components/Game.tsx

# 3. Файл вернулся к последней сохраненной версии
```

---

### Сценарий 4: Вы забыли добавить файл в коммит

```bash
# 1. Вы сделали коммит, но забыли добавить файл
git commit -m "Добавил функцию"

# 2. Добавить забытый файл
git add forgotten-file.ts

# 3. Добавить к предыдущему коммиту
git commit --amend --no-edit

# 4. Отправить (нужно force push, так как изменили историю)
git push --force
```

**⚠️ ВНИМАНИЕ:** `git push --force` используйте только если уверены! Лучше сделать новый коммит.

---

## 🔍 ПОЛЕЗНЫЕ АЛИАСЫ (сокращения)

Можно настроить короткие команды. Добавьте в файл `~/.gitconfig`:

```bash
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    unstage = reset HEAD --
    last = log -1 HEAD
    visual = !gitk
```

**Использование:**
```bash
git st    # вместо git status
git ci -m "message"  # вместо git commit -m "message"
```

---

## 📚 КРАТКАЯ ШПАРГАЛКА

### Получение изменений:
```bash
git pull              # Получить изменения
git fetch             # Посмотреть изменения (без применения)
git status            # Посмотреть статус
```

### Внесение изменений:
```bash
git status            # Посмотреть, что изменилось
git add .             # Добавить все изменения
git commit -m "..."   # Сохранить с описанием
git push              # Отправить на GitHub
```

### Просмотр:
```bash
git log               # История коммитов
git diff              # Посмотреть изменения
git diff filename.ts  # Изменения в файле
```

### Отмена:
```bash
git restore file.ts   # Отменить изменения в файле
git reset HEAD file.ts # Убрать из подготовки
```

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### "Your branch is ahead of 'origin/main'"

**Значит:** Вы сделали коммиты локально, но не отправили их.

**Решение:**
```bash
git push
```

---

### "Your branch is behind 'origin/main'"

**Значит:** На GitHub есть новые изменения, которых у вас нет.

**Решение:**
```bash
git pull
```

---

### "Please commit your changes or stash them"

**Значит:** У вас есть незакоммиченные изменения.

**Решение:**
```bash
# Вариант 1: Сохранить изменения
git add .
git commit -m "Описание"
git pull

# Вариант 2: Временно спрятать изменения
git stash
git pull
git stash pop  # Вернуть изменения обратно
```

---

### "Failed to push some refs"

**Значит:** На GitHub есть изменения, которых у вас нет.

**Решение:**
```bash
git pull
# Решить конфликты, если есть
git push
```

---

## ✅ ПРАВИЛА ХОРОШЕГО ТОНА

1. **Всегда делайте `git pull` перед началом работы**
2. **Делайте коммиты часто** (каждые 30-60 минут)
3. **Пишите понятные описания коммитов**
4. **Работайте над разными файлами**, когда возможно
5. **Обсуждайте большие изменения** перед коммитом
6. **Не коммитьте временные файлы** (используйте `.gitignore`)

---

## 🎓 ИТОГО: МИНИМУМ ДЛЯ РАБОТЫ

**Для получения изменений:**
```bash
git pull
```

**Для отправки изменений:**
```bash
git add .
git commit -m "Описание изменений"
git push
```

**Это всё, что нужно знать для начала!** Остальное можно изучить по мере необходимости. 😊

---

**Вопросы?** Спросите! 🚀

